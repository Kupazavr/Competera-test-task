1) ќбработка url -> 5 пунктовый шаблон с загрузкой на сервер(была выбрана реализаци€ через библиотеку forms с сохранением файлов на сервере из-за более быстрой скорости написани€ кода)->
-> 2 парсера дл€ csv файлов, дл€ отсекани€ возможно дублирующихс€ записей -> парсер xml со сбором в готовые  список из dict дл€ более удобного взаимодействи€ -> SELECT, UPDATE, INSERT запросы с помощью queryset
2)ѕроверка на разницу между ценой и себестоимостью(¬ т.з не было указано что это и путем дедукции так-же не было вы€вленно ничего на это похожего. ѕо этому мною был выбран вариант где delivery_cost 
означала не доставку конечному пользователю, а стоимость дл€  магазина, по этому вычисл€лась себестоимость по формуле (price - delivery_cost)) -> использование сырого sql запроса, т.к в тот момент знал как он будет выгл€деть ->
-> √рупировка дл€ получени€ " оличества товаров, добавленных в каждую из категорий по дн€м" с помощью сырого запроса, обработка и ќтправка данных по ftp на заранее полученый адресс из формы.

P.S не знаю дл€ чего были данны такие колонки как цвет и акци€, т.к не указано, что их надо добавл€ть в бд, но на вс€кий они все равно парс€тс€ и хран€тс€ во временных переменных
“есты не были написаны из-за нехватки времени и многих дыр в проэкте по которым тесты просто не пройдут, т.к что-бы их закрыть надо потратить еще как минимум пару тройку дней.
